--Initilize the Database

CREATE DATABASE programs;

CREATE TABLE program_list (
	ProgramName varchar(255),
	SizeOfBinary varchar(255),
	PathOfBinary varchar(255),
	PassedInspection varchar(255),
);

--Begin to Add Binaries

